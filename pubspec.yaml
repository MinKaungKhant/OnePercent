name: one_percent
description: Offline-first Time Tracker + Habit app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.1.1
  drift: ^2.18.0
  drift_flutter: ^0.1.0
  path_provider: ^2.1.3
  flutter_local_notifications: ^17.2.1
  intl: ^0.19.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.2
  drift_dev: ^2.18.0
  build_runner: ^2.4.11
  flutter_lints: ^4.0.0
